apiVersion: v1
kind: Service
metadata:
  name: notification-service-app
  namespace: store-crm
spec:
  type: ClusterIP
  selector:
    app: notification-service-app
  ports:
    - port: 8081
      targetPort: 8081